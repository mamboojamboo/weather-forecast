import styled from '@emotion/styled';

const StyledWrapper = styled.div`
  display: flex;
  flex-direction: column;
`;

const DaysWrapper = styled.div`
  display: flex;
  justify-content: space-between;
`;

export { StyledWrapper, DaysWrapper };
