import styled from '@emotion/styled';

const NotFounWrapper = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 100vw;
  height: 100vh;
`;

const NotFoundTitle = styled.span`
  font-size: 5rem;
`;

export { NotFounWrapper, NotFoundTitle };
